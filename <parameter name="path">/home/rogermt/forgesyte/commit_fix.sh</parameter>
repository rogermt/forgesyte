<parameter name="content">#!/bin/bash
cd /home/rogermt/forgesyte
git add -A
git commit -m "fix(core): resolve missing TaskProcessor.get_result, plugin management factory, and run_server return contract

- Added get_result() to TaskProcessor to match API/service expectations
- Restored create_plugin_management_service() factory in main.py
- Updated run_server() to explicitly satisfy NoReturn contract
- Ensures API, DI, and task orchestration layers are consistent
- Fixes test failures caused by missing DI factories and incomplete processor API"
echo "Commit complete!"
</parameter>
