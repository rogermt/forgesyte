{
  "$schema": "https://raw.githubusercontent.com/google-gemini/gemini-cli/main/schemas/settings.schema.json",
  "mcpServers": {
    "forgesyte": {
      "httpUrl": "http://localhost:8000/v1/mcp",
      "timeout": 30000,
      "description": "ForgeSyte AI-vision MCP server"
    },
     "toktoken": {
      "command": "toktoken",
      "args": ["serve"],
      "description": "Toktoken MCP Server provides precise symbol search, dependency tracking, and an MCP server. TokToken reduces LLM context token usage by 88-99% by retrieving exact code symbols instead of reading entire files"
    }
  }
}
